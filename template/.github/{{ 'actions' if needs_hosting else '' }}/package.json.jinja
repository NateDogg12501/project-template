{
  "name": "{{ project_slug }}-pipeline-stages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deploy pipeline stages for {{ project_name }}, as composite actions. Nothing here is imported by the application.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
