# CLAUDE.md

Internal notes for working on this repo. See [README.md](README.md) for
user-facing setup/run instructions, and
[../project-template/STANDARDS.md](https://github.com/NateDogg12501/project-template/blob/main/STANDARDS.md)
for the rules this project inherits from every project in this pipeline.

Scaffolded from [project-template](https://github.com/NateDogg12501/project-template),
preset `{{ flavor }}`. Capabilities this project was generated with:

| Capability | On? | What it brings here |
|---|---|---|
| API | {% if needs_api %}yes{% else %}no{% endif %} | {% if needs_api %}`backend/` (incl. `backend/test/`), `docker-compose.yml`, `scripts/setup-worktree-env.js`, the `backend` CI job{% else %}— (no server; nothing serves `frontend/`){% endif %} |
| UI | {% if needs_ui %}yes{% else %}no{% endif %} | {% if needs_ui %}`frontend/` (incl. `frontend/test/`), the `frontend` CI job{% else %}— (API-only project){% endif %} |
| HOSTED | {% if needs_hosting %}yes{% else %}no{% endif %} | {% if needs_hosting %}`terraform/` (environment-parameterized, state in the shared bucket every project uses), `backend/scripts/build-lambda.js`, the `terraform` CI job{% else %}— (local-only){% endif %} |
| DATABASE | {% if needs_datastore %}yes{% else %}no{% endif %} | {% if needs_datastore %}the DynamoDB module + IAM policy in `terraform/` (covered by the `terraform` CI job), plus `dynamodb-local` in `docker-compose.yml`, `backend/src/dynamodb.js` + `init-table.js`, and `backend/test/integration/` — its own `datastore` CI job{% else %}— (no persistent store){% endif %} |

**Turning a capability on later is a `copier update`, not a hand-build.** Edit
the flag in `.copier-answers.yml` and re-run `copier update`; the capability's
files, its tests and its CI job arrive together. Adding half of one by hand is
how a project ends up with code no CI job covers.

## What this is

{{ description }}

[Expand: what stack, local vs hosted story, the one or two things a reader
most needs to know before touching this repo.]

## Commands

```bash
{% if needs_api %}
docker compose up -d --build           # full app locally
npm test --prefix backend              # API tests (vitest + supertest)
{% endif %}
{% if needs_datastore %}
docker compose up -d --wait dynamodb-local && AWS_ENDPOINT_URL_DYNAMODB=http://localhost:8000 npm run test:integration --prefix backend  # real read/write against dynamodb-local
{% endif %}
{% if needs_ui %}
npm test --prefix frontend             # UI tests (vitest + jsdom)
{% endif %}
{% if needs_hosting %}
npm run build:lambda --prefix backend  # stage dist-lambda/ for terraform apply
terraform -chdir=terraform init -backend-config="key={{ project_slug }}/staging/terraform.tfstate"
terraform -chdir=terraform apply -var="environment=staging"  # swap staging for production to target the other env
{% endif %}
# [fill in: anything else — dev servers, data loading, one-off scripts]
```

## Architecture invariants

[Things that must stay true — the "if you change X you must also change Y"
rules. Example shape: "`backend/` is the trust boundary — the frontend's
validation is convenience only, every write is re-validated server-side."]
{% if needs_hosting %}

### Terraform derives from `terraform-modules`

`terraform/` wires pinned modules together; it does not declare AWS resources of
its own. Need something the modules don't cover? Add it to
[terraform-modules](https://github.com/NateDogg12501/terraform-modules), release
a tag, and bump every `?ref=` here together — one project pins one tag. The
`terraform` CI job fails on a `source =` that isn't a tag-pinned reference to
that repo.
{% if needs_datastore %}
The one hand-written resource here is expected, not a violation:
`aws_iam_role_policy.app_table_access` is glue between two modules — neither can
own a policy that spans both, so `lambda-web-app` exposes `lambda_role_name` for
exactly this. IAM joining two modules is the carve-out; anything a module could
own is not.
{% else %}
The only carve-out is IAM that joins two modules, and this project has none.
{% endif %}

State lives in S3 (`terraform/backend.tf`), in **one bucket shared across every
project generated from this template** — not a bucket this project owns.
`backend.tf`'s `key` is deliberately omitted and supplied at `terraform init
-backend-config="key={{ project_slug }}/<environment>/terraform.tfstate"`, since
it's the one thing that varies per apply and backend blocks can't interpolate.
Isolation from other projects sharing the bucket is this project's deploy role
being scoped to its own `{{ project_slug }}/*` prefix, not a separate bucket —
see `terraform/README.md`. Every apply also takes `-var="environment=..."`,
validated in `variables.tf` against the declared environment list; there's no
default, so an apply that doesn't say which environment fails immediately
instead of picking one silently.
{% endif %}
{% if needs_datastore %}

### The local table schema and the Terraform table schema are two definitions of one thing

`backend/src/init-table.js` creates `{{ project_slug }}-items` in DynamoDB
Local with the same key schema `terraform/main.tf`'s `module "table"` declares
for real AWS — hash key `id`, string. Nothing keeps these in sync
automatically: change one and you have to change the other by hand, or local
and deployed silently diverge. `backend/src/dynamodb.js` is the only file that
constructs a DynamoDB client — every read and write goes through it, and it
switches between DynamoDB Local and real AWS on `AWS_ENDPOINT_URL_DYNAMODB`
alone, so the application code above it never branches on where it's running.
{% endif %}

## Testing conventions

vitest, one runner across the stack. Each package runs its own tests from its
own directory, against its own `vitest.config.js` — **don't delete those config
files because they look nearly empty.** Without one, vitest searches upward,
can pick up an unrelated config, and then reports "No test files found" while
exiting 0: a green run that tested nothing.

What ships with the scaffold, all of it real and all of it passing:
{% if needs_api %}

- `backend/test/` — vitest + supertest against `createApp()` from
  `src/app.js`. `src/index.js` is the only place that binds a port, so tests
  build an app without starting a server; keep that split when you add routes.
{% endif %}
{% if needs_datastore %}

- `backend/test/integration/` — real read/write assertions against the
  actual `dynamodb-local` container, not mocks. Excluded from plain `npm
  test` (see `backend/vitest.config.js`'s `exclude`) because they need the
  container running; `npm run test:integration` runs them by hand, and the
  `datastore` CI job runs them after bringing the stack up.
{% endif %}
{% if needs_ui %}

- `frontend/test/` — vitest with the jsdom environment, driving the real
  `index.html` rather than a fixture, so a change to the markup that breaks
  `app.js` fails here.
{% endif %}

[Expand: what's tested beyond the smoke tests, and what's deliberately not.]

## Gotchas

[Bugs/surprises found only by running it, and *why*, so they don't get
silently "fixed" back in by a future session that lacks this context.]

## Things intentionally left simple

[Scope cuts, each with the reason — so a future session doesn't
"helpfully" add them back without knowing why they were cut.]

## Extending this

[The 2-3 most likely future changes and where they plug in.]

## Decisions log — standing instruction

Whenever you make or observe a hard-to-reverse decision in this repo (a
real trade-off between alternatives, a scope cut with a non-obvious reason,
a "why not the more standard approach" call) — **append an entry to
`docs/decisions.md` before ending the turn, without being asked.** This is
load-bearing: it's the only reason project decisions survive past the
session that made them. See STANDARDS.md for why.
{% if needs_api %}

## Working in a git worktree

**Run `node scripts/setup-worktree-env.js` automatically, without being
asked, whenever you set up a new worktree for this repo.** It assigns
non-colliding host ports per worktree so multiple checkouts can run
`docker compose up` concurrently. Idempotent, safe to re-run.
{% endif %}
