# docs/decisions.md

One entry per hard-to-reverse decision, logged when it's made. See
`CLAUDE.md`'s standing instruction — this file should be updated
proactively during a session, not reconstructed from memory afterward.

Format per entry:

```markdown
## YYYY-MM-DD: <short title>
**Context:** why this needed deciding
**Decision:** what was chosen
**Why not the alternative(s):** ...
**Consequence:** what this commits future work to
```

## Scaffolding
**Context:** starting {{ project_name }} from `project-template`.
**Decision:** preset `{{ flavor }}` — capabilities {% if needs_api %}API{% endif %}{% if needs_ui %}{% if needs_api %} + {% endif %}UI{% endif %}{% if needs_hosting %} + HOSTED{% endif %}{% if needs_datastore %} + DATABASE{% endif %}{% if not needs_hosting %} (local-only){% endif %}.
**Why:** {{ description }}
{% if needs_hosting %}
**Consequence:** this project's Terraform state lives in the S3 bucket shared
across every project generated from this template, keyed
`{{ project_slug }}/<environment>/terraform.tfstate` — not a bucket this
project owns or manages. Isolation from other projects in the bucket is this
project's deploy role, scoped to its own `{{ project_slug }}/*` prefix. See
`terraform/README.md`.
{% endif %}
