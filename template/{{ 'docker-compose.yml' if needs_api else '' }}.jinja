services:
  backend:
    build: ./backend
    ports:
      - "${BACKEND_PORT:-3000}:3000"
{% if needs_ui %}
    volumes:
      - ./frontend:/app/frontend
{% endif %}
    environment:
      - PORT=3000
